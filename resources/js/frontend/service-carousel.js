/**
 * ZenStyle - Services Carousel Controller
 * Pure CSS marquee animation is used for smooth, infinite circular scrolling.
 * No JS slider controls are needed.
 */

export function initServiceCarousel() {
    // Pure CSS marquee active, no manual JS positioning needed
}
